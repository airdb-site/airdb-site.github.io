# airdb.site

This repository builds the GitHub Pages homepage for the `airdb-site` organization.

During deployment, the build script calls the GitHub API, reads the organization's public
repositories, and generates a static `dist/index.html` page automatically.
